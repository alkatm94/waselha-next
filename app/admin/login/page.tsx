import Link from "next/link";import { redirect } from "next/navigation";import { loginAdmin } from "@/lib/admin-auth";import "../admin.css";
export const dynamic="force-dynamic";export const metadata={title:"دخول الإدارة | وصلها لي"};type LoginSearchParams=Promise<{error?:string}>;
export default async function Page({searchParams}:{searchParams:LoginSearchParams}){const params=await searchParams;async function loginAction(formData:FormData){"use server";const result=await loginAdmin({email:String(formData.get("email")||""),password:String(formData.get("password")||"")});if(!result.ok)redirect(`/admin/login?error=${encodeURIComponent(result.error)}`);redirect("/admin")}return <main className="admin-login" dir="rtl"><section className="admin-login-card"><div className="admin-login-brand"><div className="admin-brand-mark">و</div><div><Link href="/" className="font-black">وصلها لي</Link><p>بوابة فريق التشغيل</p></div></div><h1>دخول لوحة الإدارة</h1><p className="mt-2">مساحة آمنة ومخصصة للمديرين المخولين.</p>{params.error&&<p className="mt-4 rounded-lg bg-red-400/10 p-3 text-red-300">{params.error==="unauthorized"?"ليست لديك صلاحية كافية.":params.error}</p>}<form action={loginAction} className="mt-6"><label>البريد الإلكتروني<input name="email" type="email" required dir="ltr"/></label><label>كلمة المرور<input name="password" type="password" required dir="ltr"/></label><button>دخول آمن</button></form></section></main>}