import Link from "next/link";
import { AlertCircle, PackageCheck, Truck } from "lucide-react";
import { requireAdmin } from "@/lib/admin-auth";
import { getAdminDashboardData } from "@/lib/admin-shipments";
import { getShipmentStatusLabel } from "@/lib/shipments";
import { AdminEmptyState, AdminPageHeader, AdminPanel, AdminStatCard } from "@/components/admin/AdminUI";
import { AdminStatusBadge } from "@/components/admin/AdminStatusBadge";
export const dynamic="force-dynamic";export const metadata={title:"إدارة الشحنات | وصلها لي"};type SearchParams=Promise<{q?:string}>;
export default async function Page({searchParams}:{searchParams:SearchParams}){await requireAdmin();const p=await searchParams;const {shipments,stats}=await getAdminDashboardData(p.q||"");return <><AdminPageHeader title="الشحنات" description="متابعة الشحنات المسجلة وتشغيلها من المستودع حتى التسليم."/><div className="admin-stats-grid"><AdminStatCard label="إجمالي الشحنات" value={stats.total} icon={<Truck/>}/><AdminStatCard label="بانتظار الوصول" value={stats.awaiting} icon={<PackageCheck/>} tone="blue"/><AdminStatCard label="تحتاج إجراء" value={stats.needsAction} icon={<AlertCircle/>} tone="red"/><AdminStatCard label="جاهزة للشحن" value={stats.ready} icon={<PackageCheck/>} tone="green"/><AdminStatCard label="تم الشحن أو التسليم" value={stats.shipped} icon={<Truck/>}/></div><AdminPanel className="mt-4" title="قائمة الشحنات" description={`${shipments.length} نتيجة`}><div className="admin-toolbar"><form><input className="admin-input" name="q" defaultValue={p.q||""} placeholder="رقم الشحنة، التتبع أو العميل"/><button className="admin-search-button">بحث</button></form><span className="admin-results">عرض {shipments.length} شحنة</span></div>{shipments.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>رقم الشحنة</th><th>العميل</th><th>رقم التتبع</th><th>المنتج</th><th>الحالة</th><th>الوزن</th><th>الرسوم</th><th>تاريخ التسجيل</th><th>الإجراء</th></tr></thead><tbody>{shipments.map(s=><tr key={s.id}><td><strong dir="ltr">{s.internalReference}</strong></td><td><strong>{s.customer.name}</strong><span className="admin-ellipsis">{s.customer.email}</span></td><td dir="ltr">{s.localTrackingNumber}</td><td><span className="admin-ellipsis">{s.productName}</span></td><td><AdminStatusBadge status={s.status} label={getShipmentStatusLabel(s.status)}/></td><td>{s.weightKg?`${s.weightKg} كجم`:"-"}</td><td>{s.shippingFee?`${s.shippingFee} ريال`:"-"}</td><td>{s.createdAt.toLocaleDateString("ar-SA")}</td><td><Link className="admin-table-action" href={`/admin/shipments/${s.internalReference}`}>التفاصيل</Link></td></tr>)}</tbody></table></div>:<AdminEmptyState title="لا توجد شحنات مطابقة"/>}</AdminPanel></>}