import Link from "next/link";
import { Clock3, CircleDollarSign, CheckCircle2, ShoppingBag, XCircle } from "lucide-react";
import { requireAdmin } from "@/lib/admin-auth";
import { PURCHASE_ORDER_STATUSES, formatMoney, getAdminPurchaseOrders, getPurchaseOrderStatusLabel } from "@/lib/purchase-orders";
import { AdminEmptyState, AdminPageHeader, AdminPanel, AdminStatCard } from "@/components/admin/AdminUI";
import { AdminStatusBadge } from "@/components/admin/AdminStatusBadge";
export const dynamic="force-dynamic"; export const metadata={title:"إدارة طلبات الشراء | وصلها لي"};
type SearchParams=Promise<{q?:string;status?:string}>;
export default async function Page({searchParams}:{searchParams:SearchParams}){await requireAdmin();const params=await searchParams;const {orders,stats}=await getAdminPurchaseOrders({q:params.q,status:params.status});return <><AdminPageHeader title="طلبات الشراء" description="مراجعة الطلبات وتسعيرها ومتابعة حالاتها."/><div className="admin-stats-grid"><AdminStatCard label="إجمالي الطلبات" value={stats.total} icon={<ShoppingBag/>}/><AdminStatCard label="بانتظار المراجعة" value={stats.pending} icon={<Clock3/>} tone="red"/><AdminStatCard label="مسعّرة" value={stats.quoted} icon={<CircleDollarSign/>} tone="blue"/><AdminStatCard label="معتمدة" value={stats.approved} icon={<CheckCircle2/>} tone="green"/><AdminStatCard label="مرفوضة" value={stats.rejected} icon={<XCircle/>} tone="red"/></div><AdminPanel className="mt-4" title="قائمة الطلبات" description={`${orders.length} نتيجة`}><div className="admin-toolbar"><form><input className="admin-input" name="q" defaultValue={params.q||""} placeholder="رقم الطلب، العميل، المنتج أو المتجر"/><select className="admin-filter" name="status" defaultValue={params.status||""}><option value="">كل الحالات</option>{PURCHASE_ORDER_STATUSES.map(s=><option key={s.value} value={s.value}>{s.label}</option>)}</select><button className="admin-search-button">تطبيق</button></form><span className="admin-results">عرض {orders.length} طلب</span></div>{orders.length?<div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>رقم الطلب</th><th>العميل</th><th>المنتج / الرابط</th><th>السعر</th><th>الحالة</th><th>تاريخ الطلب</th><th>آخر تحديث</th><th>الإجراء</th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td><strong dir="ltr">{o.orderNumber}</strong></td><td><strong>{o.customer.name}</strong><span className="admin-ellipsis">{o.customer.email}</span></td><td><span className="admin-ellipsis">{o.productName}</span><a className="admin-table-link" href={o.productUrl} target="_blank" rel="noreferrer">فتح الرابط</a></td><td>{formatMoney(o.finalTotal||o.estimatedProductPrice)}</td><td><AdminStatusBadge status={o.status} label={getPurchaseOrderStatusLabel(o.status)}/></td><td>{o.createdAt.toLocaleDateString("ar-SA")}</td><td>{o.updatedAt.toLocaleDateString("ar-SA")}</td><td><Link className="admin-table-action" href={`/admin/orders/${o.orderNumber}`}>التفاصيل</Link></td></tr>)}</tbody></table></div>:<AdminEmptyState title="لا توجد طلبات مطابقة"/>}</AdminPanel></>}